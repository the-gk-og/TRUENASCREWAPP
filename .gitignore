# Ignore all contents inside uploads/ (files and subfolders)
uploads/**

# But keep the uploads/ folder itself
!uploads/
!uploads/.gitkeep  # Optional: placeholder file to keep the folder tracked

# Ignore the entire instance/ folder
instance/

# Ignore local environment variables
.env
